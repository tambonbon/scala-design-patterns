package patterns.creational.singleton

/** 
 * Singleton pattern ensures that only one object instance is created through out the application
 *    can we lazily or eagerly created, depends
 * In Scala, this can be done via `object` keyword
*/
object SingletonPattern {
  
}
